const { expect } = require('chai');
const sinon = require('sinon');
const fetch = require('node-fetch');

describe('fetchBooks', () => {
  it('fetches books and returns a promise', () => {
    // Example test structure
  });
});
